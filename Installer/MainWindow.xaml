<Window x:Class="Aetheria.Installer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="360" Width="480"
        WindowStartupLocation="CenterScreen" Background="#101116" ResizeMode="CanMinimize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Aetheria" FontSize="28" FontWeight="Bold"
                   Foreground="#E8E8F0" HorizontalAlignment="Center" Margin="0,0,0,20" />

        <StackPanel Grid.Row="1">
            <TextBlock Text="Dossier d'installation" Foreground="#A0A0B0" Margin="0,0,0,4" />
            <TextBox Text="{Binding InstallPath, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,16" />

            <CheckBox Content="Créer un raccourci sur le bureau" IsChecked="{Binding CreateDesktopShortcut}"
                      Foreground="#A0A0B0" Margin="0,0,0,20" />

            <Button Content="Installer" Command="{Binding InstallCommand}" Padding="20,10"
                    HorizontalAlignment="Center" FontWeight="Bold" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#A0F0A0" TextWrapping="Wrap" HorizontalAlignment="Center" />
            <TextBlock Text="Installation en cours..." Foreground="#A0A0B0" HorizontalAlignment="Center"
                       Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
        </StackPanel>
    </Grid>
</Window>
