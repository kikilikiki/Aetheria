<Window x:Class="Aetheria.Launcher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Aetheria.Launcher"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="700" Width="420"
        WindowStartupLocation="CenterScreen"
        Background="#101116"
        ResizeMode="CanMinimize">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis" />
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Aetheria" FontSize="32" FontWeight="Bold"
                   Foreground="#E8E8F0" HorizontalAlignment="Center" Margin="0,0,0,24" />

        <!-- Écran de connexion / inscription -->
        <StackPanel Grid.Row="1" x:Name="LoginPanel"
                    Visibility="{Binding IsLoggedIn, Converter={StaticResource InverseBoolToVis}}">
            <TextBlock Text="Pseudo ou email" Foreground="#A0A0B0" Margin="0,0,0,4" />
            <TextBox Text="{Binding UsernameOrEmail, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,12" />

            <TextBlock Text="Email (inscription uniquement)" Foreground="#A0A0B0" Margin="0,0,0,4" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,12" />

            <TextBlock Text="Mot de passe" Foreground="#A0A0B0" Margin="0,0,0,4" />
            <PasswordBox x:Name="PasswordBox" Padding="6" Margin="0,0,0,16" PasswordChanged="OnPasswordChanged" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="Se connecter" Command="{Binding LoginCommand}" Padding="16,8" Margin="0,0,8,0" />
                <Button Content="Créer un compte" Command="{Binding RegisterCommand}" Padding="16,8" />
            </StackPanel>
        </StackPanel>

        <!-- Écran de sélection de personnage -->
        <StackPanel Grid.Row="1" x:Name="CharacterPanel"
                    Visibility="{Binding IsLoggedIn, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="Choisissez votre personnage" Foreground="#A0A0B0" Margin="0,0,0,8" />
            <ListBox ItemsSource="{Binding Characters}" SelectedItem="{Binding SelectedCharacter}" Height="160">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}" FontSize="14" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                <Button Content="Jouer" Command="{Binding PlayCommand}" Padding="24,10" Margin="0,0,8,0" FontWeight="Bold" />
                <Button Content="Déconnexion" Command="{Binding LogoutCommand}" Padding="16,10" />
            </StackPanel>

            <Separator Margin="0,20,0,12" />

            <TextBlock Text="Créer un nouveau personnage" Foreground="#A0A0B0" FontWeight="Bold" Margin="0,0,0,8" />

            <TextBlock Text="Nom" Foreground="#A0A0B0" Margin="0,0,0,4" />
            <TextBox Text="{Binding NewCharacterName, UpdateSourceTrigger=PropertyChanged}" Padding="6" Margin="0,0,0,10" />

            <TextBlock Text="Classe" Foreground="#A0A0B0" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding AvailableClasses}" SelectedItem="{Binding SelectedClass}" Padding="6" Margin="0,0,0,10" />

            <TextBlock Text="Royaume" Foreground="#A0A0B0" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding AvailableKingdoms}" SelectedItem="{Binding SelectedKingdom}" Padding="6" Margin="0,0,0,12" />

            <Button Content="Créer un personnage" Command="{Binding CreateCharacterCommand}" Padding="16,8" HorizontalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#F0A0A0" TextWrapping="Wrap" HorizontalAlignment="Center" />
            <TextBlock Text="Chargement..." Foreground="#A0A0B0" HorizontalAlignment="Center"
                       Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
        </StackPanel>
    </Grid>
</Window>
