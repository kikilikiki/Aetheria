namespace Aetheria.Shared.Enums;

/// <summary>Canal d'un <c>ChatMessagePacket</c> — tchat global (tout le monde) ou tchat de guilde (membres de la même guilde uniquement).</summary>
public enum ChatChannel
{
    Global,
    Guild,
}
